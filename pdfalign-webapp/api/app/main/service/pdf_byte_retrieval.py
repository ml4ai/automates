from pdf2image import convert_from_bytes

# Need method to retrieve pdf from arxive
def retrieve_arxiv_pdf();
    pass

# Need method that handles bytes -> image conversion
def convert_bytes_to_image(bytes):
    pdf_pages = convert_from_bytes(bytes)
    populate_bboxes(filename)
    aabb_trees = dict(
            token=self.make_trees_from_boxes(self.page_boxes, "token"),
            char=self.make_trees_from_boxes(self.page_boxes, "char"),
        )
